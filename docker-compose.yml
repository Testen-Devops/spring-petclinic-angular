version: '3.6'

services:

  angular:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:8080'

  cypress:
    build:
      context: .
      dockerfile: Dockerfile-Cypress
    depends_on:
      - angular
    network_mode: 'host'